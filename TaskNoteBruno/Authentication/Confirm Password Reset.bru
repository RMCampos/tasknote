meta {
  name: Confirm Password Reset
  type: http
  seq: 8
}

post {
  url: {{addr}}/auth/complete-password-reset
  body: json
  auth: inherit
}

body:json {
  {
    "token": "string",
    "password": "string",
    "passwordAgain": "string"
  }
}

settings {
  encodeUrl: true
}
